"""attributor — marketing attribution & budget optimization pipeline.

MMM modeling + multi-touch attribution + budget optimization with block
bootstrap uncertainty quantification.
"""

from __future__ import annotations

import logging

__version__ = "1.0.0"

logging.getLogger(__name__).addHandler(logging.NullHandler())
